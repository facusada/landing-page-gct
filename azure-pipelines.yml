trigger:
  branches:
    include:
      - main

pool:
  name: 'gct-vm'

steps:
  - script: |
      cd /etc/GlobalCore/landing-page
      git pull origin main
      docker compose up --build -d
    displayName: 'Deploy landing page'
